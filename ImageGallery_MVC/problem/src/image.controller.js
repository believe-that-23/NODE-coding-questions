import multer from 'multer';
// make necessary imports here

// Implement the multer code below
const storage = multer.diskStorage({});

const upload = ;

const images = [];

function saveImage(file, description) {
}

export { upload, saveImage, images };

